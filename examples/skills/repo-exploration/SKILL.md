# Repository Exploration

Inspect the repository structure, build system, current branch state, relevant modules, tests, and ownership boundaries. Produce a concise repository context artifact that other skills can consume.

Use read-only commands first. Do not modify files.
